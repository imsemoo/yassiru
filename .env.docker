APP_NAME=يسّرو
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://localhost:8000
APP_LOCALE=ar

DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=yassiru
DB_USERNAME=yassiru
DB_PASSWORD=secret

CACHE_STORE=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis

REDIS_HOST=redis
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025

SANCTUM_STATEFUL_DOMAINS=localhost:8000

# Seeded administrator. AdminSeeder refuses to run without a password;
# there is no default, on purpose.
ADMIN_EMAIL=admin@yassiru.com
ADMIN_PASSWORD=
